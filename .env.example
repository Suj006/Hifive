DATABASE_URL="file:./dev.db"

# Secret used to sign login session cookies. Set your own random string in
# production/hosted deployments (e.g. `openssl rand -hex 32`). If left unset,
# a fixed development fallback is used — fine for local use, not for hosting.
AUTH_SECRET=""
